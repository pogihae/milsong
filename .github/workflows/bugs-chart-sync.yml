name: Bugs Chart Sync

on:
  workflow_dispatch:
    inputs:
      mode:
        description: backfill or incremental
        required: true
        default: incremental
      from:
        description: Optional start date (YYYY-MM-DD)
        required: false
      to:
        description: Optional end date (YYYY-MM-DD)
        required: false
      days:
        description: Optional number of days for incremental mode
        required: false
      wins_from_year:
        description: Optional first year for broadcast wins import
        required: false
      wins_to_year:
        description: Optional last year for broadcast wins import
        required: false
  schedule:
    - cron: '20 18 * * 6'

jobs:
  sync:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    env:
      NEXT_PUBLIC_SUPABASE_URL: ${{ secrets.NEXT_PUBLIC_SUPABASE_URL }}
      SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - run: npm ci

      - name: Run Bugs incremental sync
        if: github.event_name == 'schedule'
        run: npm run ingest:bugs:sync

      - name: Enrich Bugs metadata
        if: github.event_name == 'schedule'
        run: npm run ingest:bugs:enrich

      - name: Import broadcast wins
        if: github.event_name == 'schedule'
        run: npm run ingest:wins

      - name: Run manual sync
        if: github.event_name == 'workflow_dispatch'
        run: |
          node -e "const args = []; const input = process.env.INPUT_MODE; args.push(`--mode=${input}`); if (process.env.INPUT_FROM) args.push(`--from=${process.env.INPUT_FROM}`); if (process.env.INPUT_TO) args.push(`--to=${process.env.INPUT_TO}`); if (process.env.INPUT_DAYS) args.push(`--days=${process.env.INPUT_DAYS}`); console.log(args.join(' ')); require('node:child_process').execFileSync('npm', ['run', 'ingest:bugs', '--', ...args], { stdio: 'inherit', shell: true });"

      - name: Run manual metadata enrichment
        if: github.event_name == 'workflow_dispatch'
        run: npm run ingest:bugs:enrich

      - name: Run manual broadcast win import
        if: github.event_name == 'workflow_dispatch'
        run: |
          node -e "const args = []; if (process.env.INPUT_WINS_FROM_YEAR) args.push(`--from-year=${process.env.INPUT_WINS_FROM_YEAR}`); if (process.env.INPUT_WINS_TO_YEAR) args.push(`--to-year=${process.env.INPUT_WINS_TO_YEAR}`); console.log(args.join(' ')); require('node:child_process').execFileSync('npm', ['run', 'ingest:wins', '--', ...args], { stdio: 'inherit', shell: true });"
